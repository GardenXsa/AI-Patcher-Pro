<div align="center">

# AI Patcher Pro

**Безопасное применение кодовых патчей от языковых моделей**

[![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)](https://github.com/GardenXsa/AI-Patcher-Pro)
[![Python](https://img.shields.io/badge/python-3.8+-3776AB.svg)](https://www.python.org/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Tests](https://img.shields.io/badge/tests-46%20passing-brightgreen.svg)](tests/)

</div>

---

Инструмент для безопасного применения кодовых патчей, сгенерированных языковыми моделями (ChatGPT, Claude, DeepSeek и др.). Работает как промежуточный слой между выводом LLM и файловой системой: принимает JSON-патч, находит целевой код с помощью 5-уровневого алгоритма нечёткого поиска, показывает diff-превью и применяет изменения с автоматическим бэкапом.

---

## Содержание

- [Проблема](#проблема)
- [Демонстрация](#демонстрация)
- [Принцип работы](#принцип-работы)
- [Архитектура](#архитектура)
- [Алгоритмы поиска](#алгоритмы-поиска)
- [Формат патча](#формат-патча)
- [Примеры операций](#примеры-операций)
- [Парсер JSON](#парсер-json)
- [Система бэкапов](#система-бэкапов)
- [Безопасность](#безопасность)
- [Визуальный интерфейс](#визуальный-интерфейс)
- [Установка и запуск](#установка-и-запуск)
- [Сборка из исходников](#сборка-из-исходников)
- [Тестирование](#тестирование)
- [Технический стек](#технический-стек)
- [Лицензия](#лицензия)

---

## Проблема

Языковые модели генерируют кодовые патчи в формате JSON, но при этом регулярно допускают ошибки:

- Неточное воспроизведение существующего кода в поле `search` (лишние пробелы, другие кавычки, пропущенные строки)
- Висячие запятые в JSON (trailing commas)
- Альтернативные имена полей (`path`/`file`, `action`/`op`, `search`/`original`)
- Вставка JSON внутри Markdown-блоков с пояснительным текстом

Ручное применение таких патчей приводит к ошибкам. AI Patcher Pro решает эту проблему автоматическим нечётким поиском и валидацией каждого изменения.

---

## Демонстрация

### Полный рабочий процесс

Ниже показан полный цикл работы с AI Patcher Pro -- от вставки ответа LLM до применения патча.

```
 ШАГ 1: Вставка ответа LLM
 ╔══════════════════════════════════════════════════════════════════╗
 ║  Сырой JSON (Можно перетащить файл сюда):                      ║
 ║                                                                 ║
 ║  Вот исправление для вашего проекта:                            ║
 ║                                                                 ║
 ║  ```json                                                        ║
 ║  [                                                              ║
 ║    {                                                             ║
 ║      "path": "src/calculator.py",                                ║
 ║      "action": "replace",                                        ║
 ║      "search": "def add(a,b):\n    return a+b",                 ║
 ║      "content": "def add(a: int, b: int) -> int:\n    return a+b"║
 ║    },                                                            ║
 ║    {                                                             ║
 ║      "path": "src/calculator.py",                                ║
 ║      "action": "append",                                         ║
 ║      "content": "\ndef subtract(a: int, b: int) -> int:\n    ..."║
 ║    }                                                             ║
 ║  ]                                                              ║
 ║  ```                                                            ║
 ║                                                                 ║
 ║          [ Вставить ]  [ Очистить ]                             ║
 ║     [ Парсить и Анализировать ]                                 ║
 ╚══════════════════════════════════════════════════════════════════╝
```

```
 ШАГ 2: Парсинг и анализ
 ╔══════════════════════════════════════════════════════════════════╗
 ║  Парсер извлекает JSON из Markdown                              ║
 ║  - Удаляет текст за пределами ```json ... ```                   ║
 ║  - Автофикс висячих запятых: },  -->  }                         ║
 ║  - Нормализация полей: "file" -> "path", "op" -> "action"       ║
 ║                                                                 ║
 ║  Поисковый движок для каждой операции:                          ║
 ║  1. Точное совпадение   --> НЕ НАЙДЕНО (пробелы отличаются)     ║
 ║  2. Умные якоря         --> НЕ НАЙДЕНО                          ║
 ║  3. Игнор пробелов      --> НАЙДЕНО (совпадение без пробелов)   ║
 ║                                                                 ║
 ║  Статус: Готово к применению                                    ║
 ║  ████████████████████████████████████  2 / 2                    ║
 ╚══════════════════════════════════════════════════════════════════╝
```

```
 ШАГ 3: Diff-превью и применение
 ╔══════════════════════════════════════════════════════════════════╗
 ║  src/calculator.py  |  replace           УСПЕХ                  ║
 ║  Метод поиска: Игнор пробелов/кавычек                           ║
 ║  ─────────────────────────────────────────                      ║
 ║  - def add(a,b):                                                ║
 ║  + def add(a: int, b: int) -> int:                              ║
 ║      return a+b                                                 ║
 ║                          [Редактировать] [Копировать] [Удалить] ║
 ╠══════════════════════════════════════════════════════════════════╣
 ║  src/calculator.py  |  append             УСПЕХ                 ║
 ║  Метод поиска: Добавление в конец                               ║
 ║  ─────────────────────────────────────────                      ║
 ║  +                                                              ║
 ║  + def subtract(a: int, b: int) -> int:                         ║
 ║  +     return a - b                                             ║
 ║                          [Редактировать] [Копировать] [Удалить] ║
 ╠══════════════════════════════════════════════════════════════════╣
 ║                                                                 ║
 ║     [ История бэкапов ]  [ Отчет об ошибках для ИИ ]           ║
 ║     [        ПРИМЕНИТЬ ПАТЧ          ]                          ║
 ╚══════════════════════════════════════════════════════════════════╝
```

### Демонстрация нечёткого поиска

LLM часто искажает исходный код. Поисковый движок компенсирует это:

```
 ФАЙЛ НА ДИСКЕ:                              ИИ ПОЛАГАЕТ (search):
                                             (пробелы искажены, кавычки другие)

 1  def calculate(x, y):                    1  def calculate( x , y ):
 2      result = x + y                      2      result = x+y
 3      if result > 0:                      3      if result>0:
 4          print("positive")               4          print('positive')
 5      return result                       5      return result

 УРОВЕНЬ 1 (Точное):     НЕ НАЙДЕНО  -- пробелы и кавычки отличаются
 УРОВЕНЬ 2 (Якоря):      НЕ НАЙДЕНО  -- строк слишком мало
 УРОВЕНЬ 3 (Нормализ.):  НАЙДЕНО     -- после удаления пробелов и унификации
                                      кавычек строки идентичны

 normalize("  result = x + y  ") --> "result=x+y"
 normalize("  result = x+y  ")   --> "result=x+y"

 Нормализованные строки совпадают:
 "defcalculate(x,y):result=x+yifresult>0:print("positive")returnresult"
 "defcalculate(x,y):result=x+yifresult>0:print("positive")returnresult"

 Результат: точный фрагмент из файла возвращён для замены
```

### Демонстрация обнаружения повторного применения

Если патч уже был применён, AI Patcher Pro обнаружит это:

```
 ФАЙЛ (после первого патча):                ПОВТОРНЫЙ ПАТЧ (от ИИ):

 1  def add(a: int, b: int) -> int:        1  {
 2      return a + b                        2    "action": "replace",
 3                                          3    "path": "src/calc.py",
                                            4    "search": "def add(a,b):\n    return a+b",
                                            5    "content": "def add(a: int, b: int) -> int:\n    return a + b"
                                            6  }

 Результат поиска: content уже присутствует в файле
 Статус: ПРОПУСК -- Изменения уже присутствуют
```

---

## Принцип работы

```
+----------+     +---------------+     +----------------+     +----------+
|  Ответ   |     |  Парсер JSON  |     |  Поисковый     |     |  Diff    |
|  LLM     | --> |  (extract +   | --> |  движок        | --> |  превью  |
|          |     |   normalize)  |     |  (5 уровней)   |     |          |
+----------+     +---------------+     +----------------+     +----------+
                                                                   |
                                                                   v
                                                          +----------------+
                                                          |  Виртуальная   |
                                                          |  файловая      |
                                                          |  система       |
                                                          +----------------+
                                                                   |
                                          +------------------------+------------------------+
                                          |                        |                        |
                                          v                        v                        v
                                   +-----------+           +-----------+           +-----------+
                                   |  Бэкап    |           |  Запись   |           |  Отчёт    |
                                   |  файлов   |           |  на диск  |           |  об ошиб. |
                                   +-----------+           +-----------+           +-----------+
```

### Жизненный цикл патча

1. **Ввод** -- пользователь вставляет ответ LLM или перетаскивает файл в окно приложения
2. **Парсинг** -- извлечение JSON из Markdown, автофикс висячих запятых, нормализация полей
3. **Поиск** -- для каждой операции выполняется 5-уровневый поиск целевого кода в файле
4. **Оценка** -- операция выполняется в виртуальной файловой системе (в памяти), вычисляется diff
5. **Просмотр** -- пользователь видит карточку каждой операции: статус, метод поиска, diff-превью
6. **Применение** -- после подтверждения: бэкап оригинальных файлов, запись изменений на диск

### Виртуальная файловая система

Все операции вычисляются в памяти (`memory_files: Dict[str, str]`), без записи на диск. Это позволяет:

- Накапливать несколько операций над одним файлом корректно (каждая следующая видит результат предыдущей)
- Отменять и редактировать операции без побочных эффектов
- Гарантировать атомарность: либо применяются все операции, либо ни одной

---

## Архитектура

```
ai_patcher_pro/
    __init__.py              # Пакет, версия
    __main__.py              # Точка входа
    core/
        search.py            # 5-уровневый поисковый движок
        patcher.py           # Применение операций (replace_first_occurrence)
        json_parser.py       # Извлечение JSON из Markdown
        backup.py            # Менеджер бэкапов
        security.py          # Защита от path traversal
        processor.py         # Фоновый поток обработки
    gui/
        main_window.py       # Главное окно приложения
        operation_card.py    # Карточка операции с diff
        backup_dialog.py     # Диалог истории бэкапов
        code_viewer.py       # Подсветка синтаксиса
        styles.py            # QSS-стили (тёмная тема VS Code)
    utils/
        file_io.py           # Мульти-кодировочное чтение/запись файлов
tests/
    test_search.py           # Тесты поискового движка
    test_patcher.py          # Тесты модуля патчинга
    test_json_parser.py      # Тесты парсера JSON
    test_security.py         # Тесты безопасности
```

### Разделение ответственности

| Слой | Модули | Ответственность |
|------|--------|----------------|
| **Core** | search, patcher, processor | Бизнес-логика, не зависит от GUI |
| **GUI** | main_window, operation_card, ... | Отображение, взаимодействие с пользователем |
| **Utils** | file_io | Вспомогательные функции ввода-вывода |
| **Security** | security | Валидация путей, защита от атак |

Core-модули не импортируют GUI-модули. ProcessorThread связывает их через сигналы Qt (`pyqtSignal`), сохраняя отзывчивость интерфейса.

---

## Алгоритмы поиска

Поисковый движок (`SearchEngine`) последовательно применяет 5 уровней поиска, от точного к нечёткому. Возвращается первый успешный результат.

### Уровень 1. Точное совпадение

Проверка вхождения подстроки `search_text` в `file_text` напрямую.

```
result = search_text in file_text
```

Если найдено -- возвращается оригинальный `search_text` как есть. Это самый быстрый и надёжный путь, не требующий никаких эвристик.

**Пример:**

```
search_text: "def hello():\n    print('hello')"
file_text:   "... def hello():\n    print('hello') ..."

Результат:  "def hello():\n    print('hello')"   (точная копия)
Метод:      "Точное совпадение"
```

### Уровень 2. Умные якоря

Если ИИ исказил середину фрагмента, но первая и последняя строки уникальны в файле, блок между ними определяется как результат.

Условия срабатывания:

```
count(first_line, file) == 1  AND  count(last_line, file) == 1
```

Вычисление границ:

```
start_idx = position(first_line, file)
end_idx   = position(last_line, file) + length(last_line) + offset_to_next_newline
```

Защита от ложных срабатываний -- блок не должен превышать утроенный размер искомого фрагмента:

```
(end_idx - start_idx) < len(search_text) * 3
```

Если перенос строки после `last_line` не найден (последняя строка файла), `end_idx` устанавливается в `len(file_text)`.

**Пример:**

```
search_text от ИИ:                          Файл на диске:

1  class Calculator:                        1  class Calculator:
2      def add WRONG STUFF HERE             2      def add(self, a, b):
3          return WRONG                      3          return a + b
4      def multiply WRONG                   4      def multiply(self, a, b):
5          return WRONG                      5          return a * b
6  # End of class                           6  # End of class

Якоря:  first_line = "class Calculator:"   (1 вхождение -- уникальна)
        last_line  = "# End of class"      (1 вхождение -- уникальна)

Результат:  строки 1-6 из файла (точный фрагмент)
Метод:      "Умный поиск по якорям (начало и конец блока)"
```

### Уровень 3. Нормализованное сравнение

Удаление всех пробельных символов и унификация кавычек:

```
normalize(text):
    for each character in text:
        if character is whitespace: skip
        if character in ('"', "'", '`'): replace with '"'
        else: keep character
    return normalized_string, index_mapping
```

После нормализации выполняется поиск подстроки:

```
file_norm = normalize(file_text)
search_norm = normalize(search_text)

if search_norm in file_norm:
    return file_text[file_map[i] : file_map[i + len(search_norm) - 1] + 1]
```

`index_mapping` -- массив, связывающий индексы нормализованной строки с оригинальной, что позволяет восстановить точные границы совпадения в исходном тексте.

**Пример:**

```
Исходный файл:                Искомый текст (от ИИ):

x = "hello"                   x='hello'
y = x + ' world'              y=x+'world'

Нормализация файла:           Нормализация поиска:
x="hello"y=x+"world"          x="hello"y=x+"world"

Нормализованные строки идентичны  -->  НАЙДЕНО
Метод: "Игнор пробелов/кавычек"
```

### Уровень 4. Построчный нечёткий поиск

Сравнение скользящим окном с порогом сходства 92%.

```
search_lines = [line.strip() for line in search_text.splitlines() if line.strip()]
window_size  = len(search_lines)

for i in range(len(file_lines) - window_size + 1):
    chunk = file_lines[i : i + window_size + 2]
    ratio = SequenceMatcher(search_lines, chunk_stripped)
    if ratio > 0.92:
        return file_lines[i : end]
```

Коэффициент сходства вычисляется алгоритмом Ratcliff/Obershelp:

```
                    2 * M
    ratio = -------------------
            len(A) + len(B)

    где M -- длина наибольшей общей подпоследовательности
```

Окно расширяется на +2 строки для устойчивости к добавленным/удалённым строкам.

**Пример:**

```
Искомый текст (5 строк):          Окно в файле (5 строк + 2 запасных):

1  def process(data):             1  def process(data):
2      result = []                2      result = []
3      for item in data:          3      for item in data:       <-- ИИ добавил
4          result.append(item)    4          if item.valid:       <-- лишнюю строку
5      return result              5              result.append(item)
                                   6      return result

ratio = 2 * 180 / (195 + 210) = 0.89  --> НИЖЕ порога 0.92

Но при расширении окна на +2 строки:
ratio = 2 * 210 / (195 + 220) = 0.98  --> ВЫШЕ порога 0.92  --> НАЙДЕНО
```

### Уровень 5. Нечёткий поиск подстрок

Скользящее окно по нормализованному тексту с порогом 80%. Возвращает до 4 кандидатов для ручного выбора.

```
step = max(1, len(search_norm) // 3)
window_extra = int(len(search_norm) * 0.3)

for i in range(0, len(file_norm) - len(search_norm) + 1, step):
    window = file_norm[i : i + len(search_norm) + window_extra]
    ratio = SequenceMatcher(search_norm, window)
    if ratio > 0.80:
        longest = find_longest_match(search_norm, window)
        candidate = file_text[file_map[i + longest.b : ...]]
        suggestions.append((ratio, candidate))

return top 4 suggestions by ratio descending
```

Размер шага `len(search_norm) // 3` обеспечивает баланс между полнотой и производительностью. Расширение окна на 30% компенсирует разницу в длине между искомым и найденным фрагментами.

**Пример:**

```
ИИ ищет (нормализованно):         Файл содержит (нормализованно):
"defprocess(data):result=[]"       "defprocess(items):output=[]"

ratio = 2 * 18 / (24 + 25) = 0.73  --> НИЖЕ 0.80

Другая позиция:
"defprocess(data):result=[]"       "defprocess(data):results=[]"

ratio = 2 * 22 / (24 + 25) = 0.90  --> ВЫШЕ 0.80  --> КАНДИДАТ

Результат: список из до 4 кандидатов с процентом сходства
для ручного выбора пользователем
```

### Сводная таблица уровней поиска

| Уровень | Метод | Порог | Скорость | Точность | Когда срабатывает |
|---------|-------|-------|----------|----------|-------------------|
| 1 | Точное совпадение | 100% | Мгновенно | Идеальная | ИИ воспроизвёл код точно |
| 2 | Умные якоря | 100% (границы) | Быстро | Высокая | Первая и последняя строки уникальны |
| 3 | Игнор пробелов | 100% (норм.) | Быстро | Высокая | Отличаются только пробелы и кавычки |
| 4 | Построчный fuzzy | 92% | Средне | Средняя | Небольшие изменения в содержимом строк |
| 5 | Substring fuzzy | 80% | Медленно | Низкая | Значительные искажения, ручной выбор |

---

## Формат патча

### Поддерживаемые операции

| Действие | Описание | Обязательные поля |
|----------|----------|-------------------|
| `replace` | Замена найденного фрагмента на новый | `path`, `search`, `content` |
| `insert_after` | Вставка после найденного фрагмента | `path`, `search`, `content` |
| `insert_before` | Вставка перед найденным фрагментом | `path`, `search`, `content` |
| `delete` | Удаление найденного фрагмента | `path`, `search` |
| `append` | Добавление в конец файла | `path`, `content` |
| `prepend` | Добавление в начало файла | `path`, `content` |
| `create_file` | Создание нового файла | `path`, `content` |

### Формат JSON

Массив операций:

```json
[
    {
        "path": "src/main.py",
        "action": "replace",
        "search": "def hello():\n    print('old')",
        "content": "def hello():\n    print('new')"
    },
    {
        "path": "src/utils.py",
        "action": "create_file",
        "content": "def helper():\n    pass"
    }
]
```

Или объект с метаданными:

```json
{
    "patch_name": "Рефакторинг hello()",
    "operations": [
        { "path": "...", "action": "...", "search": "...", "content": "..." }
    ]
}
```

### Альтернативные имена полей

LLM могут использовать разные имена полей. Парсер автоматически нормализует:

| Стандартное | Альтернативы |
|-------------|-------------|
| `path` | `file` |
| `action` | `op` |
| `search` | `original`, `find` |
| `content` | `text`, `code` |

---

## Примеры операций

Ниже приведены подробные примеры для каждого типа операции. Для наглядности показано состояние файла до и после применения патча.

### replace -- Замена фрагмента

```json
{
    "path": "src/calculator.py",
    "action": "replace",
    "search": "def add(a, b):\n    return a + b",
    "content": "def add(a: int, b: int) -> int:\n    return a + b"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

  1  # calculator.py                    1  # calculator.py
  2                                     2
  3  def add(a, b):                 |   3  def add(a: int, b: int) -> int:
  4      return a + b                   4      return a + b
  5                                     5
  6  def subtract(a, b):                6  def subtract(a, b):
  7      return a - b                   7      return a - b
```

### insert_after -- Вставка после фрагмента

```json
{
    "path": "src/calculator.py",
    "action": "insert_after",
    "search": "def add(a, b):\n    return a + b",
    "content": "\ndef multiply(a, b):\n    return a * b"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

  1  def add(a, b):                     1  def add(a, b):
  2      return a + b                   2      return a + b
                                     +  3
                                     +  4  def multiply(a, b):
                                     +  5      return a * b
  3                                     6
  4  def subtract(a, b):                7  def subtract(a, b):
  5      return a - b                   8      return a - b
```

### insert_before -- Вставка перед фрагментом

```json
{
    "path": "src/calculator.py",
    "action": "insert_before",
    "search": "def subtract(a, b):",
    "content": "def multiply(a, b):\n    return a * b\n\n"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

  1  def add(a, b):                     1  def add(a, b):
  2      return a + b                   2      return a + b
  3                                  +  3  def multiply(a, b):
                                     +  4      return a * b
                                     +  5
  4  def subtract(a, b):                6  def subtract(a, b):
  5      return a - b                   7      return a - b
```

### delete -- Удаление фрагмента

```json
{
    "path": "src/calculator.py",
    "action": "delete",
    "search": "def multiply(a, b):\n    return a * b\n\n"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

  1  def add(a, b):                     1  def add(a, b):
  2      return a + b                   2      return a + b
  3                                     3
  4  def multiply(a, b):            -
  5      return a * b                -
  6                                 -
  7  def subtract(a, b):                4  def subtract(a, b):
  8      return a - b                   5      return a - b
```

### append -- Добавление в конец файла

```json
{
    "path": "src/calculator.py",
    "action": "append",
    "content": "\ndef divide(a, b):\n    if b == 0:\n        raise ValueError(\"Division by zero\")\n    return a / b"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

  1  def add(a, b):                     1  def add(a, b):
  2      return a + b                   2      return a + b
  3                                     3
  4  def subtract(a, b):                4  def subtract(a, b):
  5      return a - b                   5      return a - b
                                     +  6
                                     +  7  def divide(a, b):
                                     +  8      if b == 0:
                                     +  9          raise ValueError("Division by zero")
                                     + 10      return a / b
```

### prepend -- Добавление в начало файла

```json
{
    "path": "src/calculator.py",
    "action": "prepend",
    "content": "\"\"\"Calculator module.\"\"\"\n\n"
}
```

```
 ФАЙЛ ДО:                              ФАЙЛ ПОСЛЕ:

                                     +  1  """Calculator module."""
                                     +  2
  1  def add(a, b):                     3  def add(a, b):
  2      return a + b                   4      return a + b
  3                                     5
  4  def subtract(a, b):                6  def subtract(a, b):
  5      return a - b                   7      return a - b
```

### create_file -- Создание нового файла

```json
{
    "path": "src/utils.py",
    "action": "create_file",
    "content": "\"\"\"Utility functions.\"\"\"\n\ndef format_result(value):\n    return f\"Result: {value}\""
}
```

```
 ФАЙЛ ДО:        ФАЙЛ ПОСЛЕ:

 (не существует)   1  """Utility functions."""
                   2
                   3  def format_result(value):
                   4      return f"Result: {value}"
```

### Комбинированный пример: рефакторинг с несколькими файлами

```json
{
    "patch_name": "Добавление типизации и логирования",
    "operations": [
        {
            "path": "src/calculator.py",
            "action": "replace",
            "search": "def add(a, b):\n    return a + b",
            "content": "def add(a: int, b: int) -> int:\n    logger.info(f\"Adding {a} and {b}\")\n    return a + b"
        },
        {
            "path": "src/calculator.py",
            "action": "prepend",
            "content": "import logging\n\nlogger = logging.getLogger(__name__)\n"
        },
        {
            "path": "src/main.py",
            "action": "insert_after",
            "search": "from calculator import add",
            "content": "from calculator import subtract"
        },
        {
            "path": "tests/test_calc.py",
            "action": "create_file",
            "content": "from calculator import add\n\ndef test_add():\n    assert add(2, 3) == 5"
        }
    ]
}
```

Порядок операций над одним файлом важен: `prepend` (операция 2) выполняется после `replace` (операция 1) в виртуальной файловой системе, поэтому вставка `import logging` происходит в уже модифицированный файл.

---

## Парсер JSON

### Извлечение из Markdown

ИИ часто оборачивает JSON в Markdown-блоки с пояснениями. Парсер извлекает структуру из любого окружения:

```
Входной текст от ИИ:
+-------------------------------------------------------------------+
| Вот исправления для вашего проекта:                                |
|                                                                   |
| 1. Добавлена типизация в функцию add()                            |
| 2. Создан файл тестов                                             |
|                                                                   |
| ```json                                                           |
| [                                                                 |
|   { "path": "src/calc.py", "action": "replace", ... },            |
|   { "path": "tests/test_calc.py", "action": "create_file", ... }  |
| ]                                                                 |
| ```                                                               |
|                                                                   |
| Эти изменения безопасны и обратно совместимы.                     |
+-------------------------------------------------------------------+

Результат парсинга:
- Текст до и после JSON отбрасывается
- Извлечён массив из 2 операций
- patch_name: "Сборный патч" (автоматическое имя при отсутствии метаданных)
```

### Поддержка нескольких JSON-блоков

Если ИИ генерирует несколько блоков, они склеиваются:

```
Входной текст:
+-------------------------------------------------------------------+
| Сначала исправим баг:                                             |
| ```json                                                           |
| [{ "action": "replace", "path": "bug.py", ... }]                  |
| ```                                                               |
| А теперь добавим фичу:                                            |
| ```json                                                           |
| [{ "action": "create_file", "path": "feature.py", ... }]          |
| ```                                                               |
+-------------------------------------------------------------------+

Результат: 2 операции из разных блоков объединены в один патч
```

### Автофикс висячих запятых

LLM часто генерируют невалидный JSON с висячими запятыми. Парсер автоматически удаляет их:

```
// До фикса (невалидный JSON)
{
    "action": "replace",
    "path": "main.py",
    "search": "old code",
    "content": "new code",    <-- висячая запятая
}

// После фикса
{
    "action": "replace",
    "path": "main.py",
    "search": "old code",
    "content": "new code"
}
```

Регулярное выражение: `re.sub(r',(\s*[\]}])', r'\1', block)`

### Нормализация полей

```
Вход от ИИ:                     После нормализации:
+---------------------------+   +---------------------------+
| {                         |   | {                         |
|   "file": "main.py",      |   |   "path": "main.py",      |
|   "op": "replace",        |   |   "action": "replace",    |
|   "original": "old_code", |   |   "search": "old_code",   |
|   "code": "new_code"      |   |   "content": "new_code"   |
| }                         |   | }                         |
+---------------------------+   +---------------------------+
```

---

## Система бэкапов

Перед каждым применением патча создаётся полный бэкап затронутых файлов.

### Структура

```
.ai_backups/
    .gitignore                      # Содержит "*" -- бэкапы не коммитятся
    backup_20240115_143022/
        patch_meta.json             # {"timestamp": "...", "patch_name": "..."}
        src/
            main.py                 # Копия оригинального файла
            utils.py
    backup_20240115_150033/
        ...
```

### Восстановление

При откате файлы копируются из бэкапа в рабочую директорию с сохранением структуры:

```
backup_path/ src/main.py  -->  workspace/ src/main.py
```

`patch_meta.json` исключается из восстановления.

### Lazy loading

Диалог бэкапов загружает записи порциями по 20 штук при прокрутке, чтобы не нагружать UI при большом количестве бэкапов.

```
 Диалог "Машина Времени":
 +----------------------------------------------------------+
 | История бэкапов:                                         |
 |                                                          |
 | [20240115_150033] Добавление типизации      [ОТКАТИТЬ]  |
 | [20240115_143022] Исправление бага           [ОТКАТИТЬ]  |
 | [20240114_091511] Рефакторинг               [ОТКАТИТЬ]  |
 |                                                          |
 |                  ... прокрутка ...                       |
 |                                                          |
 | (подгружаются следующие 20 записей при прокрутке вниз)   |
 +----------------------------------------------------------+
```

---

## Безопасность

### Защита от Path Traversal

Функция `secure_path_join` проверяет, что результирующий путь находится внутри рабочей директории:

```
base_dir  = os.path.abspath(workspace)
target    = os.path.normpath(os.path.abspath(os.path.join(base_dir, relative_path)))

if target != base_dir AND NOT target.startswith(base_dir + os.sep):
    raise PermissionError
```

Использование `os.sep` после `base_dir` предотвращает совпадение префиксов (например, `/home/user` не совпадает с `/home/user10`).

Блокируемые атаки:

```
Атака                          Резолвящийся путь           Результат
---------------------------------------------------------------------------
../../etc/passwd               /etc/passwd                 PermissionError
/etc/passwd                    /etc/passwd                 PermissionError
./../../sensitive              /home/outside/sensitive     PermissionError
../other_project/main.py       /home/other_project/...     PermissionError

Нормальный запрос:
src/main.py                    /workspace/src/main.py      OK
```

### Замена только первого вхождения

Критически важное отличие от `str.replace()`, который заменяет все вхождения:

```python
# str.replace() -- ОПАСНО для патчинга
text = "import os\nimport sys\nimport os"
text.replace("import os", "import pathlib")
# Результат: "import pathlib\nimport sys\nimport pathlib"  -- ДВА вхождения!

# replace_first_occurrence() -- БЕЗОПАСНО
replace_first_occurrence(text, "import os", "import pathlib")
# Результат: "import pathlib\nimport sys\nimport os"  -- ТОЛЬКО первое
```

При наличии повторяющихся фрагментов кода (одинаковые импорты, паттерны) замена всех вхождений приводила к непреднамеренному повреждению файлов.

### Мульти-кодировочное чтение

Файлы читаются с фоллбэком по кодировкам: `utf-8` -> `cp1251` -> `latin-1`. Это необходимо для работы с legacy-файлами на Windows, где всё ещё используется cp1251.

```
Попытка 1: utf-8    --> UnicodeDecodeError
Попытка 2: cp1251   --> Успешно прочитано
```

### Обнаружение повторного применения

Если `search` не найден, но `content` уже присутствует в файле, операция помечается как `already_applied` вместо ошибки:

```
Сценарий:
1. ИИ сгенерировал патч
2. Пользователь применил патч
3. ИИ повторно сгенерировал тот же патч (галлюцинация)

Поиск:   search не найден (старый код уже заменён)
Проверка: content уже в файле
Результат: ПРОПУСК -- "Изменения уже присутствуют"
```

---

## Визуальный интерфейс

### Тёмная тема VS Code

Интерфейс выполнен в тёмной теме, вдохновлённой VS Code, с цветовым кодированием статусов:

```
Цветовая палитра:
+------------+------------+----------------------------------+
| Элемент    | Цвет       | Назначение                       |
+------------+------------+----------------------------------+
| Фон        | #1e1e1e    | Основной фон окна                |
| Сайдбар    | #252526    | Боковая панель                   |
| Акцент     | #007acc    | Кнопки действий, прогресс-бар    |
| Успех      | #27ae60    | Успешные операции, кнопка Apply  |
| Ошибка     | #c0392b    | Ошибки, кнопка Delete            |
| Предупр.   | #f39c12    | Статус ожидания                  |
| Инфо       | #3498db    | Уже применённые операции         |
| Пурпурный  | #8e44ad    | Метод поиска, бэкапы             |
+------------+------------+----------------------------------+
```

### Карточки операций

Каждая операция отображается в виде карточки с цветной рамкой, указывающей статус:

```
 +-- зелёная рамка (УСПЕХ) ----------------------------------+
 | src/calculator.py  |  replace                  УСПЕХ      |
 | Метод поиска: Игнор пробелов/кавычек                     |
 | ──────────────────────────────────────                    |
 | - def add(a, b):                                         |
 | + def add(a: int, b: int) -> int:                        |
 |      return a + b                                        |
 |                         [Редактировать] [Копировать] [Удалить] |
 +-----------------------------------------------------------+

 +-- красная рамка (ОШИБКА) ---------------------------------+
 | src/missing.py  |  replace                   ОШИБКА      |
 | Причина: Текст не найден. ИИ галлюцинирует.             |
 |                                                           |
 | Искомый текст (от ИИ):                                   |
 | +---------------------------+                             |
 | | def nonexistent():        |                             |
 | |     return None           |                             |
 | +---------------------------+                             |
 |                                                           |
 | Возможные совпадения в файле:                             |
 | +-- Вариант 1 (Совпадение: 87%) --- [Применить] -------+ |
 | | def existing():                                       | |
 | |     return None                                       | |
 | +-------------------------------------------------------+ |
 +-----------------------------------------------------------+
```

### Diff-превью

Используется формат unified diff с цветовым выделением:

```
Зелёный фон (#27ae60, 15% непрозрачности)  -- добавленные строки
Красный фон (#c0392b, 15% непрозрачности)  -- удалённые строки
Серый текст (#d4d4d4)                      -- контекстные строки
```

### Подсветка синтаксиса

Встроенный `CodeHighlighter` подсвечивает JSON, Python и JavaScript:

```
Цвета подсветки:
  Ключи JSON     -- #9cdcfe (голубой)
  Строки         -- #ce9178 (оранжевый)
  Ключевые слова -- #569cd6 (синий, жирный)
  Комментарии    -- #6a9955 (зелёный, курсив)
```

---

## Установка и запуск

### Из исходников

```bash
git clone https://github.com/GardenXsa/AI-Patcher-Pro.git
cd AI-Patcher-Pro
pip install -r requirements.txt
python -m ai_patcher_pro
```

### Готовый бинарник

```bash
chmod +x AI-Patcher-Pro-Linux-x64
./AI-Patcher-Pro-Linux-x64
```

---

## Сборка из исходников

Для сборки standalone-бинарника требуется Python 3.8+ и PyInstaller:

```bash
pip install pyinstaller
python build.py
```

Результат появится в `dist/AI-Patcher-Pro` (или `dist/AI-Patcher-Pro.exe` на Windows).

Для Windows-сборки необходимо запускать `build.py` на Windows-машине -- PyInstaller не поддерживает кросс-компиляцию.

---

## Тестирование

```bash
pip install pytest
python -m pytest tests/ -v
```

Покрытие тестами:

| Модуль | Тестов | Что проверяется |
|--------|--------|----------------|
| `search.py` | 11 | Все 5 уровней поиска, граничные случаи якорного поиска |
| `patcher.py` | 14 | replace_first_occurrence, все типы операций, нормализация полей |
| `json_parser.py` | 10 | Markdown-блоки, висячие запятые, альтернативные поля |
| `security.py` | 9 | Path traversal, пустые пути, валидация расширений |

---

## Технический стек

| Компонент | Технология |
|-----------|-----------|
| Язык | Python 3.8+ |
| GUI | PyQt6 |
| Стилизация | QSS (тёмная тема VS Code) |
| Diff-движок | `difflib` (стандартная библиотека) |
| Нечёткий поиск | `difflib.SequenceMatcher` (Ratcliff/Obershelp) |
| Сборка | PyInstaller |
| Тестирование | pytest |
| Внешние зависимости | PyQt6 -- единственная зависимость |

---

## Лицензия

MIT
